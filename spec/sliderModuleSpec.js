describe("a slider ui module suite", function () {
    var module;

    module = new UIModule;
    module.init(15, 15);


    it("test the length of the module", function () {
        expect(true).toBe(true);
    });
});